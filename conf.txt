[NETWORK]
# host is the IP addr that the server will use to bind connections
host=localhost
# port is the port that the server will use to bind connections
port=8080

[LOGS]
# logDir is the directory that the log files will be saved to. Default is current directory. 
logDir=/Users/hdot/Documents/Code/crispy-fortnight
# logLvl can be set to NORMAL (for normal operations) or DEBUG (for testing purposes)
logLvl=DEBUG

[COMMUNICATION]
# cipher can be set to "AES" or "Base64". Default is "" for NONE (plaintext)
cipher=""